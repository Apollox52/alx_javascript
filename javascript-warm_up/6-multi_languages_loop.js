const lines = ["C is fun", "Python is cool", "JavaScript is amazing"];

// Use a for loop to iterate through the array and print each line
for (let i = 0; i < lines.length; i++) {
  console.log(lines[i]);
}